{
  "name": "archon-marketplace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter \"*\" dev",
    "build": "bun run --filter \"*\" build",
    "lint": "bun run --filter \"*\" lint"
  },
  "devDependencies": {
    "@prisma/client": "6",
    "prisma": "6"
  }
}